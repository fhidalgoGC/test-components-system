const { createFolders, generateAliasConfig } = require('./create-folders');
const { logger, colors } = require('./logger');

module.exports = {
  createFolders,
  generateAliasConfig,
  logger,
  colors
};
